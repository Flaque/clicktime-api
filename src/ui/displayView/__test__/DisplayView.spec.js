import React from 'react';
import DisplayView from '../DisplayView.jsx';
import renderer from 'react-test-renderer';

test('TODO: write test for DisplayView', () => {
  expect(1).toBe(4); // Will fail.
})
